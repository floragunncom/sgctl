READ_INDEX_GROUP:
    allowed_actions:
      - "READ"
      - "SEARCH"
      - "GET"
      - "SCROLL"
      - "METADATA_READ"

ML_READ_GROUP:
  allowed_actions:
    - "READ"
    - "GET"
    - "SEARCH"
    - "METADATA_READ"

ML_WRITE_GROUP:
  allowed_actions:
    - "WRITE"
    - "INDEX"
    - "CREATE"