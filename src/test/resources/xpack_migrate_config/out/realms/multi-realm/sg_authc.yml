# https://docs.search-guard.com/latest/internal-users-database
auth_domains:

  - type: basic/internal_users_db
  - type: basic/ldap
    ldap.idp.hosts:
      - "ldaps://ad.example.com:636"      
    ldap.idp.bind_dn: "cn=ldapuser,ou=users,dc=example,dc=com"
    ldap.idp.password: "changeit"         


    ldap.user_search.base_dn: "dc=example,dc=com"
    ldap.user_search.filter.raw: "(cn=${user.name})"
