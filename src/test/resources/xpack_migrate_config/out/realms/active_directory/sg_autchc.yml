# https://docs.search-guard.com/latest/active-directory-ldap
auth_domains:
  # for native1 + file
  - type: basic/internal_users_db

  # for active directory
  - type: basic/ldap
    ldap.idp.hosts:
      - "ldaps://ad.example.com:636"
    ldap.idp.bind_dn: "es_svc_user@ad.example.com"
    ldap.idp.password: "secret-ldap-password-123"