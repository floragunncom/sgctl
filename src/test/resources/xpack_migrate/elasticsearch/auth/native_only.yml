# X-Pack elasticsearch.yml with native realm only
# Expected conversion: basic/internal_users_db

xpack:
  security:
    enabled: true
    authc:
      realms:
        native:
          native1:
            order: 0
            enabled: true
